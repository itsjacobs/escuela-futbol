package com.escuelafutbol.domain.model;

/**
 * EN: Enumerates the possible values for `Rol` in the backend module.
 * ES: Enumera los valores posibles de `Rol` en el modulo backend.
 */
public enum Rol {
    TUTOR, ADMIN
}
