package com.escuelafutbol.domain.model;

/**
 * EN: Enumerates the possible values for `EstadoPago` in the backend module.
 * ES: Enumera los valores posibles de `EstadoPago` en el modulo backend.
 */
public enum EstadoPago {
    PENDIENTE, CONFIRMADO, RECHAZADO
}
